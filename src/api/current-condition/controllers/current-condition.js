'use strict';

/**
 * current-condition controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::current-condition.current-condition');
