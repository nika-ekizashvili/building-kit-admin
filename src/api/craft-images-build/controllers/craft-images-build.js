'use strict';

/**
 * craft-images-build controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::craft-images-build.craft-images-build');
