'use strict';

/**
 * craft-images-build router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::craft-images-build.craft-images-build');
