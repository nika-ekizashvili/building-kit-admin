'use strict';

/**
 * craft-images-build service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::craft-images-build.craft-images-build');
